import {StyleSheet} from 'react-native';
import {THEME} from '../../theme';

export const styles = StyleSheet.create({
  tabStyle: {
    height: 70,
  },
  titleStyle: {
    fontSize: 20,
  },
  addButton: {
    position: 'absolute',
    bottom: 10,
    right: 10,
  },
});
